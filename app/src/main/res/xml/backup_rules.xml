<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude shared preferences for sensitive data -->
    <exclude domain="sharedpref" path="nutrition_prefs.xml" />
    <!-- Include database for backup -->
    <include domain="database" path="." />
</full-backup-content>